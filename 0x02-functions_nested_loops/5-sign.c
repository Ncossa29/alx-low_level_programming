#include "main.h"
/**
 * print_sigh - prototype
 * description: output sigh of number
 * @n: int to be checked
 * Return: retuns 1 if number = to zero
 */
int print_sigh(int n)
{
	if (n > 0)
	{
		_putchar('+');
		return (1);
	} else if (n == 0)
}
